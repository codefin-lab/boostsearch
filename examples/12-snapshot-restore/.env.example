# Copy to .env and edit. run.sh reads .env if it is there.
#
# Where the server is. `make serve` starts one here.
BS=http://127.0.0.1:9272

# the only directory a filesystem snapshot repository may be created under
BOOSTSEARCH_PATH_REPO=/tmp/boost-repo

# the repository name this example uses
REPO=backups
