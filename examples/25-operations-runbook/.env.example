# Copy to .env and edit. run.sh reads .env if it is there.
#
# Where the server is. `make serve` starts one here. The example assumes a
# single node: on a cluster with two data nodes the replica in step 1 would be
# placed, and the steps about a yellow cluster would have nothing to show.
VS=http://127.0.0.1:9285
