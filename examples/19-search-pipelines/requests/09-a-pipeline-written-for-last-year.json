{
  "description": "Written when books still carried legacy_sku; no document has it now",
  "response_processors": [
    { "rename_field": { "tag": "old-sku", "field": "legacy_sku", "target_field": "sku" } },
    { "rename_field": { "tag": "price-for-old-apps", "field": "price_eur", "target_field": "price" } }
  ]
}
