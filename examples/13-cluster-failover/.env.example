# Copy to .env and edit. run.sh reads .env if it is there.
#
# Where the server is. `make serve` starts one here.
# this example starts its own three nodes and ignores VS

# where the three nodes put their data and logs
ROOT=/tmp/velo-cluster-example

# the binary to start
BIN=../../target/release/velosearch
