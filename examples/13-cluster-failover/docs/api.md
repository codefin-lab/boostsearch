# API surface -- 13. Three nodes, and one of them killed

Every request this example makes, in the order it makes them. Generated
from `run.sh`; if the two disagree, `run.sh` is right.

| Step | Method | Path | Body |
|---|---|---|---|
| three nodes, which find each other by their transport addresses | `GET` | `/_cluster/health?wait_for_nodes=3&wait_for_status=green&timeout=30s` | inline |
|  | `GET` | `/_cat/nodes?v&h=name,node.role,cluster_manager,http,version` | inline |
| who is the cluster manager, and what the cluster state says | `GET` | `/_cluster/state/cluster_manager_node,nodes` | inline |
|  | `GET` | `/_cat/cluster_manager?v` | inline |
| an index with a replica, so a copy of every shard is on another node | `PUT` | `/orders` | inline |
|  | `GET` | `/_cluster/health/orders?wait_for_status=green&timeout=30s` | inline |
|  | `GET` | `/_cat/shards/orders?v&h=index,shard,prirep,state,docs,node` | inline |
| write through every node, not just one | `GET` | `/orders/_count` | inline |
| kill n3 while it holds copies | `GET` | `/_cluster/health?timeout=20s` | inline |
|  | `GET` | `/_cat/shards/orders?v&h=index,shard,prirep,state,node` | inline |
| the cluster still answers, and still takes writes | `POST` | `/orders/_doc/after-the-loss?refresh=true` | inline |
|  | `GET` | `/orders/_count` | inline |
| why a shard is where it is -- or is not anywhere | `POST` | `/_cluster/allocation/explain` | inline |
| bring n3 back: it is filled from the primaries rather than starting empty | `GET` | `/_cluster/health?wait_for_status=green&timeout=60s` | inline |
|  | `GET` | `/_cat/recovery/orders?v&h=index,shard,type,stage,source_node,target_node,files_percent` | inline |
|  | `GET` | `/_cat/shards/orders?v&h=index,shard,prirep,state,docs,node` | inline |
| every copy agrees on the count -- ask each node for its own | `GET` | `/orders/_search?preference=_local` | inline |
| search across the whole cluster, and see which shards answered | `GET` | `/orders/_search` | inline |
| moving a shard by hand | `POST` | `/_cluster/reroute?explain=true` | inline |
| settings that live in the cluster rather than on one node | `PUT` | `/_cluster/settings` | inline |
|  | `GET` | `/_cluster/settings?flat_settings=true` | inline |
| what each node is doing | `GET` | `/_cat/nodes?v&h=name,heap.percent,ram.percent,cpu,load_1m,node.role` | inline |
|  | `GET` | `/_nodes/stats/indices?filter_path=nodes.*.name,nodes.*.indices.docs` | inline |
|  | `GET` | `/_cat/allocation?v` | inline |

## Endpoints touched

Path parameters and query strings removed, deduplicated:

- `/_cat/allocation`
- `/_cat/cluster_manager`
- `/_cat/nodes`
- `/_cat/recovery/orders`
- `/_cat/shards/orders`
- `/_cluster/allocation/explain`
- `/_cluster/health`
- `/_cluster/health/orders`
- `/_cluster/reroute`
- `/_cluster/settings`
- `/_cluster/state/cluster_manager_node,nodes`
- `/_nodes/stats/indices`
- `/orders`
- `/orders/_count`
- `/orders/_doc/after-the-loss`
- `/orders/_search`

## Request bodies

Every body in this example is either written inline in `run.sh` or
generated by the Python block inside it.
