# Copy to .env and edit. run.sh reads .env if it is there.
#
# Where the server is. `make serve` starts one here.
BS=http://127.0.0.1:9274

# which hosts _reindex may read from; only the last step needs it
BOOSTSEARCH_REINDEX_ALLOWLIST=127.0.0.1:*
