{
  "what": "OpenSearch Dashboards 3.1.0 measured against its own test/api_integration suite on the 3.1 branch, three runs. These are the cases the released server does not pass, so they are not cases a replacement can be held to.",
  "runs": 3,
  "totals": {
    "cases": 166,
    "passing_best": 140,
    "pending": 2
  },
  "failing": [
    "apis Telemetry /api/telemetry/v2/clusters/_stats application usage limits 10k + 1 should only use the first 10k docs for the application_usage data (they'll be rolled up in a later process)",
    "apis Telemetry /api/telemetry/v2/clusters/_stats application usage limits basic behaviour should return application_usage data",
    "apis Telemetry /api/telemetry/v2/clusters/_stats should pull local stats and validate data types",
    "apis Telemetry /api/telemetry/v2/clusters/_stats should pull local stats and validate fields",
    "apis Telemetry /api/telemetry/v2/optIn API \"after each\" hook for \"should support sending false with allowChangingOptInStatus true\"",
    "apis Telemetry /api/telemetry/v2/optIn API should support sending false with allowChangingOptInStatus true",
    "apis Telemetry /api/telemetry/v2/userHasSeenNotice API Telemetry User has seen OptIn Notice should update telemetry setting field via PUT",
    "apis UI Metric ui_metric API supports multiple events",
    "apis Workspace CRUD apis \"after each\" hook for \"create\"",
    "apis Workspace CRUD apis create",
    "apis core compression doesn't use compression when there is a non-allowlisted referer",
    "apis home apis sample data apis install parameters should load opensearch index containing sample data with dates relative to now parameter",
    "apis home apis sample data apis install should load opensearch index containing sample data with dates relative to current time",
    "apis home apis sample data apis install should return 200 if success",
    "apis index_patterns index_patterns/service/lib index_patterns/* error handler \"before all\" hook in \"index_patterns/* error handler\"",
    "apis saved objects management apis find meta attributes injected properly \"before all\" hook for \"should inject meta attributes for searches\"",
    "apis saved objects management apis relationships \"before all\" hook in \"relationships\"",
    "apis saved objects management apis scroll_count filters on title when `searchString` is provided",
    "apis saved objects management apis scroll_count includes all requested types even when none match the search",
    "apis saved objects management apis scroll_count only returns count for types to include",
    "apis saved objects management apis scroll_count returns the count for each included types",
    "apis stats opensearch-dashboards stats api basic should return the stats without cluster_uuid with 'extended' query string param = false",
    "apis stats opensearch-dashboards stats api basic should return the stats without cluster_uuid with no query string params",
    "apis stats opensearch-dashboards stats api extended legacy should return return the 'extended' data in the old format with 'legacy' query string param present",
    "apis stats opensearch-dashboards stats api extended should return the stats, cluster_uuid, and usage with 'extended' query string param = true",
    "apis stats opensearch-dashboards stats api extended should return the stats, cluster_uuid, and usage with 'extended' query string param present"
  ],
  "failing_every_run": [
    "apis Telemetry /api/telemetry/v2/clusters/_stats application usage limits 10k + 1 should only use the first 10k docs for the application_usage data (they'll be rolled up in a later process)",
    "apis Telemetry /api/telemetry/v2/clusters/_stats application usage limits basic behaviour should return application_usage data",
    "apis Telemetry /api/telemetry/v2/clusters/_stats should pull local stats and validate data types",
    "apis Telemetry /api/telemetry/v2/clusters/_stats should pull local stats and validate fields",
    "apis Telemetry /api/telemetry/v2/optIn API \"after each\" hook for \"should support sending false with allowChangingOptInStatus true\"",
    "apis Telemetry /api/telemetry/v2/optIn API should support sending false with allowChangingOptInStatus true",
    "apis Telemetry /api/telemetry/v2/userHasSeenNotice API Telemetry User has seen OptIn Notice should update telemetry setting field via PUT",
    "apis UI Metric ui_metric API supports multiple events",
    "apis Workspace CRUD apis \"after each\" hook for \"create\"",
    "apis Workspace CRUD apis create",
    "apis core compression doesn't use compression when there is a non-allowlisted referer",
    "apis home apis sample data apis install should return 200 if success",
    "apis index_patterns index_patterns/service/lib index_patterns/* error handler \"before all\" hook in \"index_patterns/* error handler\"",
    "apis saved objects management apis find meta attributes injected properly \"before all\" hook for \"should inject meta attributes for searches\"",
    "apis saved objects management apis relationships \"before all\" hook in \"relationships\"",
    "apis saved objects management apis scroll_count filters on title when `searchString` is provided",
    "apis saved objects management apis scroll_count includes all requested types even when none match the search",
    "apis saved objects management apis scroll_count only returns count for types to include",
    "apis saved objects management apis scroll_count returns the count for each included types",
    "apis stats opensearch-dashboards stats api basic should return the stats without cluster_uuid with 'extended' query string param = false",
    "apis stats opensearch-dashboards stats api basic should return the stats without cluster_uuid with no query string params",
    "apis stats opensearch-dashboards stats api extended legacy should return return the 'extended' data in the old format with 'legacy' query string param present",
    "apis stats opensearch-dashboards stats api extended should return the stats, cluster_uuid, and usage with 'extended' query string param = true",
    "apis stats opensearch-dashboards stats api extended should return the stats, cluster_uuid, and usage with 'extended' query string param present"
  ],
  "failing_some_runs": [
    "apis home apis sample data apis install parameters should load opensearch index containing sample data with dates relative to now parameter",
    "apis home apis sample data apis install should load opensearch index containing sample data with dates relative to current time"
  ]
}
